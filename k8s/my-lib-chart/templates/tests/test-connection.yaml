apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "my-lib-chart.fullname" . }}-test-connection"
  labels:
    {{- include "my-lib-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "my-lib-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
