apiVersion: apps/v1
kind: Deployment
metadata:
  name: moscow-time-cs
  namespace: default
  labels:
    app: moscow-time-cs
spec:
  selector:
    matchLabels:
      app: moscow-time-cs
  replicas: 3
  strategy:
    rollingUpdate:
      maxSurge: 40%
      maxUnavailable: 40%
    type: RollingUpdate
  template:
    metadata:
      annotations:
        kubectl.kubernetes.io/default-container:  moscow-time-cs
      labels:
        app: moscow-time-cs
    spec:
      containers:
      - name: moscow-time-py
        image: efimpuzhalov/moscow-time-cs-app:distroless
        imagePullPolicy: IfNotPresent
        resources:
          requests:
            cpu: 100m
            memory: 100Mi
          limits:
            cpu: 100m
            memory: 100Mi
        ports:
        - containerPort: 80
          name: moscow-time-cs
      restartPolicy: Always
---