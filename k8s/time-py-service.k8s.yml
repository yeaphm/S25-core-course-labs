apiVersion: v1
kind: Service
metadata:
  name: moscow-time-py-service
  namespace: default
  labels:
    app: moscow-time-py
spec:
  selector:
    app: moscow-time-py
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: NodePort
